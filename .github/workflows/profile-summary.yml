name: Generate Profile Summary Cards

on:
  schedule:
    - cron: "0 18 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4

      - name: Generate profile summary cards
        uses: vn7n24fzkq/github-profile-summary-cards@release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          USERNAME: ${{ github.repository_owner }}

      - name: Keep github_dark + github_light themes, drop stats radar
        run: |
          if [ -d "profile-summary-card-output" ]; then
            cd profile-summary-card-output
            find . -mindepth 1 -maxdepth 1 -type d \
              ! -name 'github_dark' ! -name 'github_light' -exec rm -rf {} +
            rm -f github_dark/3-stats.svg github_dark/README.md
            rm -f github_light/3-stats.svg github_light/README.md
            rm -f README.md
            cd ..
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git pull --rebase --autostash origin main
            git add -A
            if ! git diff --cached --quiet; then
              git commit -m "chore: refresh profile summary cards (dark + light)"
              git push origin main
            fi
          fi
